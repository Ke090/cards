# 検証記録

検証日: 2026-09-19 / Windows / Node.js 22.17.0 / npm 10.9.2。

## リポジトリ準備
- GitHubからclone。デフォルトブランチは `main`。`git pull --ff-only` で最新状態を確認。
- `codex/feature-gacha-game` を作成し、変更はすべてこのブランチで実施。
- 初期ファイルはREADME.mdのみ。package.json、lock、ソース、CI、AGENTS.md、API、DB、認証、環境変数設定、既存コーディング規約は存在しなかった。
- 実装前にAGENTS.mdを新規作成し、新規採用の技術・コマンド・変更方針・Git運用・ユーザー確認を含む完了条件を明記。

## 自動チェック
- `npm run lint`: 成功。初回の起動ヘルパーのCommonJS importエラーはES Modulesへの統一で修正。
- `npm run typecheck`: 成功。
- `npm test`: 27件成功。確率の境界、レア、重複所持、保存の往復、不正な保存データを検証。
- `npm run build`: 成功。
- 依存インストール時の監査: 脆弱性0件。初期Vitestの警告は修正版への更新で解消。
- GitHub Actionsに同じ4チェックを設定。リモート上のCIは未実行。

## ローカル起動とブラウザー
- `cmd /c start-local.bat --no-open` でlockからのインストールと127.0.0.1:5173起動が成功。検証時だけブラウザー自動起動を抑止。
- 通常抽選、もう一度ひく、結果から一覧への移動、未獲得時の空表示、獲得済みフィルターを確認。
- 重複時の所持数増加、再読み込み後の復元、24%×4種類＋4%の提供割合を確認。
- 実際のランダム抽選でレアを獲得し、専用配色・結果表示・5/5コンプリートを確認。
- Enterで抽選、Escapeでダイアログを閉じる操作を確認。
- 320px・390px・768px・通常デスクトップで確認。装飾による横はみ出しを修正し、documentのscrollWidthがclientWidthを超えないことを確認。
- 画像の縦方向の見切れと隣のキャラクターの写り込みをCSSの表示範囲で調整。
- ブラウザーコンソールの警告・エラーは検証時0件。
- 中断前はブラウザー自動承認が利用制限で失敗したが、再開後は接続と確認が成功。

## 残る確認・仕様上の制限
- ユーザーによるローカル動作確認は未完了。PR作成・mergeは行わない。
- 通常のbatダブルクリック時の既定ブラウザー自動起動、OS設定切り替えによる演出短縮、演出中の連打をユーザー環境でも確認する。
- 保存は同じブラウザー・同じorigin限定。端末間同期はなく、複数タブ同時プレイは想定しない。
- 保存拒否・容量不足の実ブラウザー再現は未実施（例外処理と通知は実装済み）。
- 失敗中の自動テストはない。
